# Stage 4 — simulate_strategy execution dependencies.
#
# Core service is installable WITHOUT this file. Install on the
# Railway service(s) that run simulate_strategy:
#
#     pip install -r requirements.txt -r requirements-stage4.txt
#
# ───────────────────────────────────────────────────────────────
# REQUIRES PYTHON 3.12+ — pandas-ta on PyPI now requires
# Requires-Python >=3.12. The service as a whole has been moved to
# Python 3.12 (see Dockerfile + .python-version). The git-URL
# workaround that previously lived here has been removed — Railway's
# default builder lacks git https auth for github.com in the sandbox
# we tested, so the git-URL install blocked on build. PyPI is the
# more reliable path on Py 3.12.

pandas-ta>=0.4.71b0
# Pinned to 2026.3 — the Stage 4.5 Railway egress probe revealed that
# build_config's required-keys set is tied to this exact release
# (``exit_pricing`` and friends were introduced then). If we ever
# upgrade this pin, the quant/tests/execution/test_freqtrade_config.py
# smoke test MUST be re-run, because freqtrade's SCHEMA_TRADE_REQUIRED
# is not considered part of its public API and has changed between
# minors.
freqtrade==2026.3

# NOTE — Stage 4.5 reproducible-install sweep. Original Stage-1 pins
# (e.g. fastapi==0.115.3) are incompatible with the current openbb
# ecosystem (openbb-core>=1.6 requires fastapi>=0.128). These versions
# are the tightest set that resolves cleanly with pip 26.x on
# Python 3.11 against PyPI as of April 2026. If a future resolver
# sweep fails, the failure mode is almost always openbb-core's
# fastapi / uvicorn floor moving again — bump those first.

openbb==4.7.1
pyqlib==0.9.6
mplfinance==0.12.10b0
supabase==2.8.1
pandas>=2.2.3,<3.0    # pin below 3.x; many transitive libs (mplfinance,
                      # scikit-image) haven't migrated to pandas 3 APIs yet.
pydantic>=2.9.2,<3.0  # fastapi 0.128 pulls pydantic 2.13+; loosen floor
                      # so the resolver can pick the right patch release.
python-dotenv>=1.0.1
httpx>=0.27.0
tenacity>=9.0.0
scipy>=1.14.1

# API + MCP server
fastapi>=0.128.0,<0.130
uvicorn[standard]>=0.34.2,<0.45
mcp>=1.1.3,<2.0

# Charting (Stage 2). Pillow comes transitively via mplfinance but pin
# explicitly for the visual-regression test. scikit-image powers SSIM
# behind the optional --strict-visual flag.
Pillow==10.4.0
scikit-image==0.24.0

# NOTE — Stage 4 (freqtrade + pandas-ta) lives in requirements-stage4.txt.
# The core service stays installable without it. Install Stage 4 only on
# the Railway service(s) that run simulate_strategy:
#     pip install -r requirements.txt -r requirements-stage4.txt

# Test
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1

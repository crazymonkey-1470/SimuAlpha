{
  "name": "simualpha",
  "version": "1.0.0",
  "private": true,
  "description": "SimuAlpha monorepo — Node backend + React frontend. Used by Railway for a single-service deploy that serves the built frontend from the backend.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:all":   "npm --prefix backend ci && npm --prefix frontend ci",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend":  "echo 'no backend build step (node runtime)'",
    "build:all":      "npm run install:all && npm run build:frontend",
    "start":          "node backend/server.js",
    "dev:frontend":   "npm --prefix frontend run dev",
    "dev:backend":    "npm --prefix backend run dev",
    "test":           "npm --prefix backend test"
  }
}
